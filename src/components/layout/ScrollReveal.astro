---
export interface Props {
  class?: string;
  [key: string]: unknown;
}

const { class: className = '', ...attrs } = Astro.props;
---

<div class={`reveal ${className}`} {...attrs}>
  <slot />
</div>
